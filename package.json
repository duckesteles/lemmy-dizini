{
  "name": "lemmy-dizini",
  "version": "1.0.0",
  "private": true,
  "description": "Modular, static directory of Turkish Lemmy communities.",
  "license": "AGPL-3.0-only",
  "type": "commonjs",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && python3 -m http.server 8080 --bind 0.0.0.0 -d dist"
  },
  "engines": {
    "node": ">=18"
  }
}
